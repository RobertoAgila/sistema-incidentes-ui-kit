{
  "name": "helpdesk-backend-api",
  "version": "1.0.0",
  "description": "API REST para la gestión de tickets de incidentes con Node.js, Express y MongoDB.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node src/seed.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/config/database.js && node --check src/models/Ticket.js && node --check src/controllers/ticketController.js && node --check src/routes/ticketRoutes.js && node --check src/middlewares/errorHandler.js && node --check src/seed.js"
  },
  "keywords": [
    "nodejs",
    "express",
    "mongodb",
    "mongoose",
    "api-rest",
    "tickets",
    "helpdesk"
  ],
  "author": "Estudiante",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.3.0",
    "mongoose": "^9.7.4",
    "morgan": "^1.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
